# Autoresearch Airflow umbrella chart용 dev values 예제입니다.
# Infrastructure-owned values such as namespace, Workload Identity, and secrets
# should be finalized in SKYAHO/Autoresearch-infra.

airflow:
  images:
    airflow:
      repository: asia-northeast3-docker.pkg.dev/<gcp-project>/autoresearch-dev-docker/autoresearch-airflow
      tag: replace-me
      pullPolicy: IfNotPresent

  # metadata DB는 관리형 Cloud SQL을 외부 연결로 사용한다(내장 pod PostgreSQL 미사용).
  # 신규 환경에서는 아래 이름의 k8s Secret을 배포 전에 생성해야 한다.
  # key "connection" = <SECRET: see docs/cloud-sql-metadata.md for format>
  # 상세 절차: docs/cloud-sql-metadata.md
  postgresql:
    enabled: false

  data:
    metadataSecretName: airflow-metadata-db

  dags:
    persistence:
      enabled: false
    gitSync:
      enabled: true
      repo: https://github.com/SKYAHO/Autoresearch-airflow.git
      branch: main
      rev: HEAD
      depth: 1
      subPath: dags
      period: 30s

  extraEnv: |
    - name: AIRFLOW_VAR_YOUTUBE_BACKFILL_SOURCE_PATH
      valueFrom:
        secretKeyRef:
          name: autoresearch-airflow-env
          key: YOUTUBE_BACKFILL_SOURCE
          optional: true

  env:
    - name: AIRFLOW__CORE__LOAD_EXAMPLES
      value: "False"
    # 관리형 metadata DB의 최대 연결 수 제약 대비 연결 풀을 작게 유지한다.
    - name: AIRFLOW__DATABASE__SQL_ALCHEMY_POOL_SIZE
      value: "3"
    - name: AIRFLOW__DATABASE__SQL_ALCHEMY_MAX_OVERFLOW
      value: "3"
    - name: AIRFLOW__DATABASE__SQL_ALCHEMY_POOL_RECYCLE
      value: "1800"
    # Google provider 오퍼레이터(GCS 센서, BigQuery 오퍼레이터)가 사용하는
    # 기본 connection. 키 파일 없이 실행 파드의 Workload Identity(ADC)를
    # 사용하므로 URI에 자격 증명을 담지 않는다.
    - name: AIRFLOW_CONN_GOOGLE_CLOUD_DEFAULT
      value: "google-cloud-platform://"
    - name: AIRFLOW_VAR_YOUTUBE_LAKE_BUCKET
      value: "<gcs-bucket-name-without-gs-prefix>"
    - name: AIRFLOW_VAR_AUTORESEARCH_BATCH_IMAGE
      value: "asia-northeast3-docker.pkg.dev/<gcp-project>/autoresearch-dev-docker/autoresearch-batch@sha256:<production-digest>"
    - name: AIRFLOW_VAR_AUTORESEARCH_TRAINING_IMAGE
      value: "asia-northeast3-docker.pkg.dev/<gcp-project>/autoresearch-dev-docker/autoresearch-training@sha256:<production-digest>"
    - name: AIRFLOW_VAR_AUTORESEARCH_FEAST_IMAGE
      value: "asia-northeast3-docker.pkg.dev/<gcp-project>/autoresearch-dev-docker/autoresearch-feast@sha256:<production-digest>"
    - name: AIRFLOW_VAR_FEAST_CODE_ARTIFACTS_BUCKET
      value: "<gcs-code-artifacts-bucket-name-without-gs-prefix>"
    - name: AIRFLOW_VAR_FEAST_GCP_PROJECT_ID
      value: "<gcp-project>"
    - name: AIRFLOW_VAR_FEAST_BQ_DATASET
      value: "feast_offline_store"
    - name: AIRFLOW_VAR_FEAST_BQ_LOCATION
      value: "asia-northeast3"
    - name: AIRFLOW_VAR_FEAST_GCS_REGISTRY_PATH
      value: "gs://<feast-registry-bucket>/registry.db"
    - name: AIRFLOW_VAR_FEAST_GCS_STAGING_LOCATION
      value: "gs://<feast-staging-bucket>/"
    - name: AIRFLOW_VAR_FEAST_REDIS_HOST
      value: "<redis-cluster-discovery-endpoint>"
    - name: AIRFLOW_VAR_FEAST_REDIS_PORT
      value: "6379"
    - name: AIRFLOW_VAR_FEAST_REDIS_CA_SECRET_ID
      value: "<redis-server-ca-secret-id>"
    - name: AIRFLOW_VAR_AIRFLOW_KPO_NAMESPACE
      value: "airflow"
    - name: AIRFLOW_VAR_AIRFLOW_KPO_SERVICE_ACCOUNT
      value: "autoresearch-batch"
    - name: AIRFLOW_VAR_AUTORESEARCH_API_SECRET_NAME
      value: "autoresearch-airflow-env"
    - name: AIRFLOW_VAR_YOUTUBE_TRENDING_BASE_PATH
      value: "gs://<gcs-bucket-name-without-gs-prefix>/data_lake/youtube_trending_kr"
    - name: AIRFLOW_VAR_YOUTUBE_BACKFILL_OUTPUT_BASE_PATH
      value: "gs://<gcs-bucket-name-without-gs-prefix>/data_lake/youtube_trending_kr"
    - name: AIRFLOW_VAR_YOUTUBE_TRENDING_REGION_CODE
      value: "KR"
    - name: AIRFLOW_VAR_YOUTUBE_TRENDING_MAX_RESULTS
      value: "200"
    - name: AIRFLOW_VAR_ACTION_LOG_GENERATOR
      value: "openrouter"
    - name: AIRFLOW_VAR_ACTION_LOG_MODEL_NAME
      value: "mistralai/mistral-nemo"
    - name: AIRFLOW_VAR_ACTION_LOG_YOUTUBE_BASE_PATH
      value: "gs://<gcs-bucket-name-without-gs-prefix>/data_lake/youtube_trending_kr"
    - name: AIRFLOW_VAR_ACTION_LOG_VIRTUAL_USERS_PATH
      value: "gs://<gcs-bucket-name-without-gs-prefix>/asset/virtual_user/vu_1000.parquet"
    - name: AIRFLOW_VAR_ACTION_LOG_OUTPUT_DIR
      value: "gs://<gcs-bucket-name-without-gs-prefix>/data_lake/action_log"
    - name: AIRFLOW_VAR_ACTION_LOG_QUARANTINE_DIR
      value: "gs://<gcs-bucket-name-without-gs-prefix>/data_lake/action_log_quarantine"
    - name: AIRFLOW_VAR_ACTION_LOG_SHARD_WORK_DIR
      value: "gs://<gcs-bucket-name-without-gs-prefix>/data_lake/action_log_work"
    - name: AIRFLOW_VAR_ACTION_LOG_SHARD_QUARANTINE_DIR
      value: "gs://<gcs-bucket-name-without-gs-prefix>/data_lake/action_log_quarantine_work"
    - name: AIRFLOW_VAR_ACTION_LOG_PROGRESS_DIR
      value: "gs://<gcs-bucket-name-without-gs-prefix>/data_lake/action_log_progress"
    - name: AIRFLOW_VAR_ACTION_LOG_CHECKPOINT_DIR
      value: "gs://<gcs-bucket-name-without-gs-prefix>/data_lake/action_log_checkpoints"
    - name: AIRFLOW_VAR_ACTION_LOG_SHARD_COUNT
      value: "5"
    - name: AIRFLOW_VAR_ACTION_LOG_CANDIDATES_PER_USER
      value: "24"
    - name: AIRFLOW_VAR_ACTION_LOG_TARGET_CTR
      value: "0.02"
    - name: AIRFLOW_VAR_ACTION_LOG_PERSONALIZED_RATIO
      value: "0.7"
    - name: AIRFLOW_VAR_ACTION_LOG_POPULAR_RATIO
      value: "0.2"
    - name: AIRFLOW_VAR_ACTION_LOG_EXPLORATION_RATIO
      value: "0.1"
    - name: AIRFLOW_VAR_ACTION_LOG_SEED
      value: "42"
    - name: AIRFLOW_VAR_ACTION_LOG_MAX_CONCURRENCY
      value: "3"
    - name: AIRFLOW_VAR_ACTION_LOG_CHUNK_SIZE
      value: "24"
    - name: AIRFLOW_VAR_ACTION_LOG_MAX_QUARANTINE_RATIO
      value: "0.5"
    - name: AIRFLOW_VAR_ACTION_LOG_OPENROUTER_POOL
      value: "action_log_openrouter"
    - name: AIRFLOW_VAR_OPENROUTER_TIMEOUT_SEC
      value: "60"
    - name: AIRFLOW_VAR_OPENROUTER_MAX_RETRIES
      value: "2"
    - name: AIRFLOW_VAR_OPENROUTER_TIMEOUT_MAX_RETRIES
      value: "1"
    - name: AIRFLOW_VAR_OPENROUTER_RETRY_BACKOFF_BASE_SEC
      value: "1"
    - name: AIRFLOW_VAR_OPENROUTER_RETRY_BACKOFF_MAX_SEC
      value: "30"
    - name: AIRFLOW_VAR_OPENROUTER_ALLOW_FALLBACKS
      value: "true"
    - name: AIRFLOW_VAR_OPENROUTER_REQUIRE_PARAMETERS
      value: "true"

  scheduler:
    # Google provider 오퍼레이터는 scheduler 파드에서 직접 실행된다. 아래 GSA는
    # Autoresearch-infra에서 airflow/airflow-scheduler KSA와 IAM으로 연결한다.
    serviceAccount:
      annotations:
        iam.gke.io/gcp-service-account: <airflow-scheduler-gsa>@<gcp-project>.iam.gserviceaccount.com

  migrateDatabaseJob:
    args:
      - bash
      - -c
      - |
        airflow db migrate
        airflow pools set action_log_openrouter 5 "Action log OpenRouter shard fan-out"
