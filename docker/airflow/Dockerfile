FROM quay.io/astronomer/astro-runtime:13.8.0

# DAGs and their helper module are delivered together by git-sync. The Airflow
# runtime image intentionally contains no application source or DAG helper copy.
